import { UserButton } from '@clerk/nextjs'
import React from 'react'

const Home = () => {
  return (
    <div>
    </div>
  )
}

export default Home

//UserButton afterSignOutUrl is used so that the user could have a ui for sign out.
